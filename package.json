{
  "devDependencies": {
    "@types/node": "^22.7.4",
    "esbuild": "^0.23.1"
  },
  "type": "module",
  "scripts": {
    "prepublishOnly": "node --run build",
    "build": "node --run build:bin && node --run build:lib",
    "build:bin": "esbuild --outfile=bin.js --format=esm --platform=node --bundle src/bin.ts",
    "build:lib": "esbuild --outfile=lib.js --format=esm --bundle src/lib.ts",
    "test": "node --test **/*.test.ts && node run-test.ts"
  },
  "bin": {
    "html-template-compiler": "./bin.js"
  },
  "types": "./src/lib.ts",
  "exports": {
    ".": {
      "import": "./lib.js",
      "types": "./src/lib.ts"
    }
  },
  "author": "Sam Thorogood <sam.thorogood@gmail.com>",
  "license": "Apache-2.0",
  "name": "html-template-compiler",
  "version": "0.1.0",
  "sideEffects": false
}
